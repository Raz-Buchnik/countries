{
  "name": "razaviv-countries-common",
  "version": "1.0.7",
  "description": "Common interfaces and methods for the project",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "clean": "rm -rf dist",
    "pub:minor": "npm run build && npm version minor && npm publish",
    "pub:major": "npm run build && npm version major && npm publish",
    "pub:patch": "npm run build && npm version patch && npm publish"
  },
  "author": "Raz Buchnik",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^27.0.1",
    "jest": "^27.2.1",
    "nodemon": "^2.0.13",
    "ts-jest": "^27.0.5"
  },
  "dependencies": {
    "mongodb": "^4.1.3",
    "razaviv-common": "^1.0.42"
  }
}
